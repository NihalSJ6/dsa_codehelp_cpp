{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "C++ Debug (LLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${fileDirname}",
      "preLaunchTask": "C/C++: g++ build active file",
      "terminal": "integrated"
    }
  ]
}
